import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles({
	textCenter: {
		textAlign: "center",
	},
	topDivider: {
		marginBottom: "6rem",
	},
})

export default useStyles
