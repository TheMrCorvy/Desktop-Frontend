import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles({
	link: {
		textDecoration: "none",
		color: "inherit",
	},
	listItem: {
		paddingTop: 20,
		paddingBottom: 20,
	},
})

export default useStyles
